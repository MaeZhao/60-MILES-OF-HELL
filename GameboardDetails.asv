%% Initializing GBoard variables
clear
clc
figure('WindowStyle', 'modal'); 
Gboard = cell(10,10);
grass = imread('grass_texture.png');
for r = 1 :10
    for c = 1:10
    Gboard{r,c} = grass;
    end
end
function s = helpMe(row)
if row == 10
    return "[Gboard(10)]";
end
imshow([Gboard(row) 
    
%%imshow([r]; functio
